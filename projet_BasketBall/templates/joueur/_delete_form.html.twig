<form method="post" action="{{ path('joueur_delete', {'id_joueur': joueur.getIdJoueur}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ joueur.getIdJoueur) }}">
    <button class="btn">Supprimer</button>
</form>
