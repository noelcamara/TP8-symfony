{% extends 'base.html.twig' %}

{% block title %}Equipe{% endblock %}

{% block body %}
    <h1>Equipe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_equipe</th>
                <td>{{ equipe.idEquipe }}</td>
            </tr>
            <tr>
                <th>Nom_equipe</th>
                <td>{{ equipe.nomEquipe }}</td>
            </tr>
            <tr>
                <th>Nb_victoire</th>
                <td>{{ equipe.nbVictoire }}</td>
            </tr>
            <tr>
                <th>Nb_defaite</th>
                <td>{{ equipe.nbDefaite }}</td>
            </tr>
            <tr>
                <th>Nb_points</th>
                <td>{{ equipe.nbPoints }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('equipe_index') }}">back to list</a>

    <a href="{{ path('equipe_edit', {'id_equipe': equipe.id_equipe}) }}">edit</a>

    {{ include('equipe/_delete_form.html.twig') }}
{% endblock %}
